name: Daily GitHub Trending Report

on:
  schedule:
    # 每天UTC时间9:00运行（北京时间17:00）
    - cron: '0 9 * * *'
  # 支持手动触发（在GitHub页面点击运行）
  workflow_dispatch:
    inputs:
      test_mode:
        description: '测试模式（不发送到Telegram）'
        required: false
        default: 'false'
        type: boolean

jobs:
  generate-trending-report:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'  # 改为更稳定的3.9版本
        
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        echo "✅ Python依赖安装完成"
        
    - name: Run GitHub Trending Reporter
      env:
        # Telegram配置（从GitHub Secrets读取）
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        
        # Tavily API（可选）
        TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
        
        # GitHub Token（自动提供）
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
        # 测试模式控制
        TEST_MODE: ${{ inputs.test_mode }}
      run: |
        echo "🚀 开始生成GitHub Trending简报..."
        echo "运行模式: ${{ github.event_name }}"
        echo "触发者: ${{ github.actor }}"
        echo "Python版本: $(python --version)"
        
        # 运行脚本
        python github_trending_actions.py
        
        echo "✅ 脚本执行完成"
        
    - name: Upload logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: trending-report-error-logs
        path: |
          *.log
        retention-days: 7